name: 'README AI'
description: 'Auto-generate README from commits'

inputs:
  openai-api-key:
    required: true
  github-token:
    required: true
    default: ${{ github.token }}

runs:
  using: 'docker'
  image: 'Dockerfile'